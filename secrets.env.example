# Copy to secrets.env and fill in. secrets.env is gitignored.
TELEGRAM_BOT_TOKEN=
# Optional overrides (defaults: Virtuals router key from ccr config, GLM-5-turbo)
# Required unless ~/.claude-code-router/config.json exists (operator convenience):
LLM_API_KEY=
#LLM_URL=https://compute.virtuals.io/v1/chat/completions
#SPONSOR_MODEL=z-ai-glm-5-turbo
